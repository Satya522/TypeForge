'use client';

/**
 * AuroraBackground — Ambient flowing light aurora.
 * Adds cinematic depth without performance cost.
 * Pure CSS animation, no JS overhead.
 */
export default function AuroraBackground() {
  return <div className="aurora-bg" aria-hidden="true" />;
}
