'use client';

/**
 * VignetteOverlay — Cinematic darkened edge effect.
 * Creates a subtle vignette around the viewport for depth.
 * Used by film/cinema and premium apps like Arc Browser.
 */
export default function VignetteOverlay() {
  return <div className="vignette-overlay" aria-hidden="true" />;
}
