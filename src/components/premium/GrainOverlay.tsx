'use client';

/**
 * GrainOverlay — Film grain texture for premium tactile feel.
 * Adds subtle noise texture like Apple.com and Linear.app.
 * Purely decorative, zero interactivity impact.
 */
export default function GrainOverlay() {
  return <div className="premium-grain" aria-hidden="true" />;
}
